{% extends "base/base_component.py.jinja2" %}

{% block description %}{{ description | default(name + ' interface definition') }}{% endblock %}

{% block layer %}Core Interfaces{% endblock %}

{% block responsibilities %}    - Define {{ name }} contract via Protocol
    - Specify method signatures for implementations
    - Enable dependency injection and testing{% endblock %}

{% block stdlib_imports %}
{{ super() }}
from typing import Protocol, Any, List, Optional, Dict
{% endblock %}

{% block content %}
class {{ name }}(Protocol):
    """{{ docstring | default(description) }}"""

    {% if methods %}
    {% for method in methods %}
    def {{ method.name }}(self, {{ method.params | default('') }}) -> {{ method.return_type | default('None') }}:
        """{{ method.docstring | default('Method definition.') }}"""
        ...

    {% endfor %}
    {% else %}
    # TODO: Define methods
    # def method_name(self, arg: type) -> return_type:
    #     ...
    ...
    {% endif %}
{% endblock %}

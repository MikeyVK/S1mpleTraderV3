# ST3 Workflow MCP Server Configuration
# ======================================

server:
  name: "st3-workflow"
  version: "1.0.0"
  workspace_root: "D:/dev/SimpleTraderV3"

logging:
  level: "DEBUG"
  audit_log: "mcp_server/logs/mcp_audit.log"

github:
  owner: "MikeyVK"
  repo: "S1mpleTraderV3"
  project_number: 1
  # token: Set via GITHUB_TOKEN environment variable
